import { StyleSheet } from 'react-native';
import {
  colors,
} from '../../styles';

export default StyleSheet.create({
  recordingsContainer: {
    backgroundColor: colors.shade0,
    height: '100%',
    marginTop: 20,
  },
});
